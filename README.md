# AI-Tools Repo (Agent Sync & Utilities) 🤖

本仓库包含一组专为 AI Agent 开发和使用设计的管理工具，旨在简化多个 AI 引擎（如 Claude Code, Gemini CLI, Antigravity, OpenCode 等）之间的 Skill (技能) 同步与资源管理。

## 🛠 核心工具

### 1. Agent Sync (`agent-sync`)
> 原 `ai-tools`

**Agent Sync** 是一个全能环境同步工具，负责保持你的 Agent 工作流一致。

- **🚀 一键同步 (Link)**: 自动将你的 Skill 源码分发挂载到所有主流 AI Agent 工具的配置目录中。
- **🔄 强制更新 (Update)**: 深度扫描所有 AI 相关目录，自动识别 Git 仓库或 Skill 源码，并强制同步远端（自动处理冲突）。
- **🔍 状态监控 (List)**: 清晰展示当前各工具加载的 Skill 列表、指向路径以及链接有效性。
- **🧹 智能清理 (Cleanup)**: 移除失效的软链接。

**支持的工具环境：**
- Claude Code (`~/.claude`)
- Gemini CLI (`~/.gemini`)
- Antigravity (`~/.antigravity`)
- OpenCode (`~/.opencode`)
- 其他主流 AI IDE 插件

**用法:**
```bash
# 进入交互模式
./agent-sync

# 或直接运行命令
./agent-sync update
./agent-sync link ~/my-skills
```

### 2. CC Prune (`cc-prune`)
> 原 `cc_clean_provider.sh`

**CC Prune** 是一个专门针对 CC Switch 数据库的清理实用程序。

- **功能**: 安全列出并删除（Prune）数据库中的 Provider 记录及其关联的 Endpoints。
- **安全**: 每次操作前自动创建数据库备份。
- **用法**:
  ```bash
  ./cc-prune
  ```

## 📦 安装

```bash
git clone git@github.com:ly87ing/ai-tools.git
cd ai-tools
chmod +x agent-sync cc-prune
```

## 💡 命名说明

- **Agent Sync**: 强调其核心价值在于跨不同 Agent 环境“同步”状态。
- **CC Prune**: 采用业界标准的 "Prune" 术语，意为修剪/清理不再使用的资源。

---
*Generated by Gemini CLI Manager*
